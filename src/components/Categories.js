import React, { Component } from 'react';

class Categories extends Component {
  state = { }

  render() {
    return (
      <div className="categories">
        <button type="button" className="bookbtn">Check status</button>
      </div>
    );
  }
}

export default Categories;
